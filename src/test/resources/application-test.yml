logging:
  level:
    com.task.managerapi: DEBUG
    org.springframework: WARN
    org.hibernate: WARN
    root: INFO

  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

  file:
    name: logs/test-application.log
    path: ./logs